Author: Yingbin Liang

Assignment 1

Problem 4: 
  1. the getCourse() method uses ArrayList<>() data structure to dynamically create a collection of Strings, which are names of the four courses.
  2. use nextInt(int n) method in Random() class to generate an random integer in 1 … 3
  3. use the random number in ArrayList.get(int n) method to return an element of random index in the ArrayList

Problem 5:
  1. modified the if condition for checking illegal input (n < 0 instead of 0 <= 0)
  2. modified the if condition for checking base cases (n < 2 instead of n <= 2)
  3. modified the recursion based on the property of Fibonacci sequence (plus instead of minus)

Problem 6:

Are we supposed to, just like problem 5, write a method that takes an Integer as argument and returns a reversed number (e.g., "getReverseNumber(Integer num)"), and ONLY test this method in ReverseNumberTest.java? In this case, I suppose we would use java.util.Scanner outside of getReverseNumber(Integer num) method (maybe in a constructor or main method) and thus the Scanner instance is NOT required to be tested in ReverseNumberTest.java. 

I am confused because you said "You can safely assume that the expected input to your class ReverseNumber is an integer (int)". When you say "input", are you referring to user input through java.util.Scanner or argument for the constructor of class ReverseNumber? If my above assumption is true, then the constructor of class ReverseNumber will take NO argument, and instead will use Scanner class to provide argument for getReverseNumber(Integer num). 